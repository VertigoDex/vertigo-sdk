import { describe, it, expect } from "vitest";

describe("SwapClient (v3)", () => {
  describe("migration note", () => {
    it("should note that SwapClient has been replaced with swap helper", () => {
      expect(true).toBe(true);
    });
  });
});

import { describe, it, expect } from "vitest";

describe("VertigoAPI (v3)", () => {
  describe("migration note", () => {
    it("should note that VertigoAPI has been removed from v3", () => {
      expect(true).toBe(true);
    });
  });
});
